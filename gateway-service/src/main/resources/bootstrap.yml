spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    config:
    uri: http://localhost:8888
    profile: ${SPRING_PROFILES_ACTIVE:dev}
